#include <stdio.h>
#include <stdlib.h>

mian()
{
	int N, M, X=1, aux, valida, repetido;
	do
	{
	printf("digite o numero total de figurinhas no album (entre 1 e 100): ");
	scanf("%d", &N);
	if(N<1||N>100) printf("numero nao pertence ao intervalo.");	
	}while(N<1||N>100);
	do
	{
	printf("digite o numero de figurinhas já compradas (entre 1 e 300): ");
	scanf("%d", &M);
	if(M<1||M>300) print("numero nao pertence ao intervalo.");	
	}while(M<1||M>300);
	
	do
	{	do
		{
		printf("digite o numero da figurinha: ");
		if(x<1||X>M)printf("numero invalido.");
		}while(x<1||X>M);
	
	
	
	}
	
	
	
}
